<UserControl x:Class="Szofttech_WPF.View.ChatGUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Szofttech_WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="chatLog" Text="{Binding ChatMessages}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#3269a8" IsReadOnly="True"/>
        <TextBox x:Name="chatInput" Text="{Binding ChatInput, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="1" 
                 HorizontalAlignment="Stretch" 
                 VerticalAlignment="Stretch">
            <TextBox.InputBindings>
                <KeyBinding Command="{Binding SendCommand}" Key="Return"/>
            </TextBox.InputBindings>
        </TextBox>
        <Button Content="Gomb" Command="{Binding SendCommand}" Width="100" Grid.Row="1"/>
        
            
    </Grid>
</UserControl>
